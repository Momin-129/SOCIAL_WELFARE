@model List<ServicePlus.Models.Entities.Service>

<div class="container-fluid d-flex align-items-center auto-height my-5">
    <div class="container shadow-lg p-3 bg-dark">
        <input type="text" id="searchInput" class="form-control mb-3 bg-dark text-white"
            placeholder="Search by Service Name">
        <div class="table-responsive">
            <table class="table table-dark table-striped table-bordered border-info" style="table-layout: fixed;">
                <thead>
                    <tr>
                        <td class="text-white">S.No.</td>
                        <td class="text-white">Service Name</td>
                        <td class="text-white">Department Name</td>
                        <td class="text-white">State</td>
                    </tr>
                </thead>
                <tbody id="dynamic-list">

                </tbody>
            </table>
        </div>
        <!-- Pagination component -->
        <nav aria-label="Page navigation">
            <ul class="pagination" id="pagination"></ul>
        </nav>
    </div>
</div>


<script>
    // Serialize the server-side model to JSON and assign it to a JavaScript variable
    const serviceList = @Html.Raw(Json.Serialize(Model));
</script>
<script src="~/js/User/services.js"></script>